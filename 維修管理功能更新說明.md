# 維修管理功能更新說明

更新日期：2026-06-11

## 2026-06-11 補充更新：清除舊示範維修紀錄

這次依照實際使用需求，已將舊的無效報修示範資料清除。

目前報修管理頁不會再預設載入 `MOCK_REPAIRS` 的舊資料，頁面會從沒有維修紀錄的狀態開始。

如果瀏覽器 `localStorage` 之前已經存過舊的示範維修資料，系統載入時也會自動清理，只保留手動新增的維修紀錄。

保留條件：

- `id` 或 `task_id` 以 `MR` 開頭
- 或資料中有 `onsite_notice: true`

也就是說，之後由客服在系統內手動 key in 的維修資料才會保留在畫面上。

## 2026-06-11 部署紀錄

已發佈到 GitHub Pages：

```text
https://active716.github.io/Bravo-House-rental-sysyem/
```

部署 repo：

```text
https://github.com/active716/Bravo-House-rental-sysyem
```

部署 commit：

```text
00586e9 Add maintenance record workflow
```

部署後已確認 GitHub Pages 首頁載入新版資源：

- `app.js?v=603`
- `style.css?v=603`
- 維修登錄表 `repairForm`

## 2026-06-11 雲端同步準備版

這次針對「對方新增維修，你這邊看不到」的問題，已開始改成多人共用資料架構。

已建立 Google Sheet 維修資料庫：

```text
https://docs.google.com/spreadsheets/d/1IPcwCNKbCRVz9JsvQYeYhZ4qQnEPzQZza8WE081VcJ0/edit
```

資料分頁：

```text
repairs
```

已新增 Apps Script 後端程式：

```text
apps-script/Code.gs
```

已新增部署說明：

```text
apps-script/部署維修同步後端.md
```

前端 `app.js` 已調整成：

- 未設定 GAS URL 時：維修資料仍使用本機 localStorage，網站照常可用。
- 設定 GAS URL 後：維修新增、讀取、完成/未完成狀態會走 Google Sheet 雲端同步。
- 這次 GAS 只同步維修紀錄，不影響房客、帳單、水電等其他模組。

目前還差最後一步：

```text
把 Apps Script 部署成 Web App，取得 URL 後貼回 app.js 的 GAS_WEB_APP_URL。
```

拿到 Web App URL 後，重新發佈 GitHub Pages，就能正式啟用多人同步。

## 2026-06-11 雲端同步啟用版

已接上 Apps Script Web App URL：

```text
https://script.google.com/macros/s/AKfycbwDzpxuTFkUdQxzBEgdIzETqenKlDcKpN_AjKjG2IIRkls7BHZGNEF78lbbro-dJ2Pv/exec
```

前端資源版本已更新：

```text
app.js?v=605
style.css?v=605
```

已完成後端驗證：

- `GET action=getRepairs` 回傳 `ok:true`
- JSONP 讀取格式正常
- 臨時 POST 新增維修測試正常
- 測試資料已從 Google Sheet 刪除，正式資料庫保持乾淨

正式啟用後，維修紀錄會集中存在 Google Sheet：

```text
Bravo House 維修紀錄資料庫 / repairs
```

## 2026-06-11 同步寫入修正

使用者回報對方新增後仍無法同步。

驗證結果：

- Google Sheet `repairs` 分頁只有表頭，沒有新增資料。
- GAS `GET action=getRepairs` 正常。
- GAS JSONP 讀取正常。
- GAS POST 後端本身可寫入。

判斷：

```text
問題出在 GitHub Pages 前端瀏覽器送出到 Apps Script 的跨網域寫入路徑。
```

修正：

- 將維修寫入改為 `navigator.sendBeacon()`。
- 讀取仍維持 JSONP。
- 新增 / 完成後的雲端重抓延後到 1.8 秒，讓 Apps Script 有時間寫入 Sheet。
- 前端資源版本更新為 `app.js?v=606`。

部署後複驗：

- 已發佈 commit：`0eb953c Use beacon for repair sync writes`
- GitHub Pages 首頁已確認載入 `app.js?v=606`。
- 線上 `app.js` 已確認使用正式 Apps Script URL。
- 送出臨時測試維修 `MR_VERIFY_SYNC_DELETE_1781165004` 後，Apps Script 回傳 `ok:true`。
- 透過 `GET action=getRepairs` 已確認測試維修可被網站讀取。
- 測試列已從 Google Sheet 刪除。
- 刪除後再次讀取，`repairs` 與 `tasks` 均回到空陣列，資料庫保持乾淨。

## 2026-06-12 跨網域同步修正

使用者回報不同網域 / 不同電腦仍無法同步。

判斷：

```text
Apps Script Web App 雖然可以 POST，但不同使用者瀏覽器環境下仍可能被跨網域寫入、重新導向或安全策略影響。
```

修正：

- 維修新增 / 完成狀態更新改走 JSONP GET 寫入。
- 讀取與寫入都使用同一種跨網域相容方式，降低不同電腦不同網域的限制。
- `apps-script/Code.gs` 的 `doGet` 已支援 `action=upsert`。
- 前端只會同步真正的維修案件，避免合約聯絡等其他 `tasks` 誤寫入維修表。
- 前端資源版本更新為 `app.js?v=607`。
- 已再更新為 `app.js?v=608`，當 Apps Script 尚未重新部署時，畫面會直接提示「維修同步後端尚未更新，請重新部署 Apps Script Web App」。

重要：

```text
這版需要把 apps-script/Code.gs 貼回 Apps Script，並用「管理部署 / 編輯現有部署」重新部署 Web App。
只推 GitHub Pages 不足以讓後端支援 JSONP 寫入。
```

部署後複驗：

- 2026-06-12 已確認原 Web App URL 回應新版 `action=upsert`。
- 非維修表測試回傳 `ok:true` 與 `skipped:true`，代表 `doGet` 已支援新版流程。
- 透過 JSONP GET 新增臨時測試維修 `MR_JSONP_VERIFY_DELETE_1781259919` 成功。
- 透過 `GET action=getRepairs` 已確認網站讀取得到該測試維修。
- 測試列已從 Google Sheet 刪除。
- 刪除後再次讀取，測試維修已不存在；既有正式資料如 `六合館201`、`六合館301` 仍存在。

## 2026-06-12 完成紀錄自動清除

為避免 Google Sheet 長期累積過多已完成維修紀錄，同時保留誤按完成時的緩衝期，採用以下規則：

```text
完成紀錄保留 7 天，超過 7 天後自動從雲端清除。
```

選擇 7 天的原因：

- 不建議完成後立刻刪除，避免現場人員誤按完成後無法追查。
- 7 天通常足夠處理客服回查、住戶追問或現場更正。
- 只保留最近完成紀錄，可避免 Google Sheet 無限累積。

實作位置：

- `apps-script/Code.gs`：每次讀取或寫入維修資料時，自動清除完成超過 7 天的紀錄。
- `index.html`：報修管理頁新增提示文字。
- `style.css`：新增提示文字樣式。

前端資源版本更新為：

```text
app.js?v=609
style.css?v=609
```

## 這次更新目標

在原本的租賃管理系統中，補上客服可以登錄維修案件的功能，讓現場人員能在系統內看到待處理項目，並將案件標記為完成。

## 已完成範圍

### 1. 報修管理頁新增「維修紀錄表」

位置：`index.html`

在「報修管理」頁面新增一個客服登錄表單，欄位包含：

- 房號
- 問題類型
- 登錄人
- 故障敘述

送出後會新增一筆維修紀錄，狀態預設為「未完成」。

### 2. 狀態簡化為「未完成 / 完成」

位置：`app.js`

維修狀態目前收斂成兩種：

- 未完成
- 完成

原本舊資料中的 `pending`、`in_progress` 都會視為「未完成」，避免舊資料漏掉。

### 3. 新增「現場人員通知」區塊

位置：`index.html`、`app.js`

在報修管理頁新增現場人員通知清單，會列出目前尚未完成的維修案件。

現場人員可以直接在通知清單中按「完成」，案件會更新為完成狀態。

### 4. 儀表板與側邊欄數字同步

位置：`app.js`

新增或完成維修案件後，以下資訊會同步更新：

- 儀表板待處理維修提醒
- 報修管理 KPI
- 側邊欄報修數字
- 現場人員通知數量

### 5. 本機資料儲存

位置：`app.js`

目前新增的維修紀錄會先存在瀏覽器的 `localStorage`。

使用的 key：

```text
rental_demo_state_v2
```

如果未來要串接 Google Sheet / GAS，資料結構已沿用現有的 `tasks` 表概念，可以再接到後端儲存。

### 6. 舊紀錄清理

位置：`app.js`

已新增清理邏輯：

- 不再預設載入舊的 `MOCK_REPAIRS`
- 自動移除 localStorage 裡的舊示範維修紀錄
- 只保留手動新增的維修紀錄

## 使用方式

1. 進入系統後點選左側「報修管理」。
2. 在「新增維修紀錄」區塊選擇房號。
3. 選擇問題類型。
4. 輸入故障敘述。
5. 按「新增並通知現場」。
6. 現場人員可在右側「現場人員通知」看到未完成案件。
7. 現場人員處理完後按「完成」。

## 修改檔案

| 檔案 | 說明 |
|---|---|
| `index.html` | 新增維修登錄表、現場人員通知區塊、報修狀態篩選 |
| `app.js` | 新增維修資料、狀態判斷、通知清單渲染、完成狀態更新 |
| `style.css` | 新增維修表單與現場通知清單樣式 |

## 驗證結果

已完成以下檢查：

- `app.js` 使用 JS VM 語法解析通過。
- 用假 DOM runtime 測試新增維修資料，資料筆數會增加。
- 測試新增後狀態為「未完成」。
- 測試標記完成後，狀態會變成「完成」。
- 測試資料會寫入 `localStorage`。
- 2026-06-11 補測：確認舊 `MOCK_REPAIRS` 不再作為預設資料。
- 2026-06-11 補測：確認 localStorage 舊示範紀錄會被過濾，只保留手動新增紀錄。

## 目前限制

目前的「通知」是系統內的待處理清單，不是 LINE、簡訊或手機推播。

也就是說，現場人員需要進入系統的「報修管理」頁查看待處理案件。

## 後續可擴充

如果之後要繼續做，可以依序補：

1. 串接 Google Sheet / GAS，讓維修紀錄不只存在單一瀏覽器。
2. 加上現場人員角色或專用頁面。
3. 串接 LINE Notify、LINE Bot 或其他通知方式。
4. 加上照片上傳。
5. 加上維修完成時間、處理人員、處理備註。
