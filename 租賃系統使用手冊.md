# 🏠 租賃自動化與帳單系統 — 系統使用手冊

本手冊說明「2026自動化帳單系統」的完整架構、日常操作流程以及與 LINE 官方帳號機器人的互動機制，供管理團隊與系統開發參考。

---

## 一、 系統核心架構

本系統由三大模組整合而成：
1.  **Google 試算表（資料庫核心）**：儲存所有房客、電表、帳單與系統設定。
2.  **網頁管理後台（Web App）**：視覺化管理界面，進行數據同步、報表檢視與日常管理。
3.  **LINE 官方帳號機器人（LINE OA）**：房客自助綁定、自動發送每月帳單。

---

## 二、 Google 試算表資料表說明

試算表共包含以下工作表，請勿隨意修改欄位名稱：

### 1. `config` (系統設定)
*   `yyyymm`：目前計費月份（例如 `2026-05`，可用公式改為自動更新）。
*   `kwh_price`：每度電費單價（預設為 `5` 元）。
*   `due_day`：每月繳費截止日（預設為 `10` 日）。
*   `due_message`、`reminder_1`、`reminder_2`：發送給房客帳單下方的提醒文案。

### 2. `tenants` (房客名冊)
*   儲存各館房號、房客姓名、租金、已綁定的 `line_user_id`（未綁定則為空）及狀態（`active` 代表承租中）。

### 3. `meter` (電表記錄)
*   記錄每月的電表讀數。包含 `yyyymm`、房號、`prev_kwh` (上期度數)、`curr_kwh` (本期度數)、`used_kwh` (使用度數) 與異常警示。

### 4. `invoices` (帳單明細)
*   系統自動計算產生的月度帳單，包含租金、電費、水費（依人數計，每人 100 元）、總計金額與繳費狀態。

### 5. `src_tenant` 與 `src_meter` (外部資料對接)
*   透過 `IMPORTRANGE` 公式，即時同步管理員在外部「原始抄表登記表」所填寫的最新資料。

### 6. 公司戶帳務輔助表（漢民公司適用）
公司戶與一般個人房客不同，不應要求每一間房都各自綁定 LINE 或各自產生一張發送帳單。系統需保留原本個人房客流程，同時新增公司戶輔助表，專門處理「多房間、單一帳務窗口、每房水電明細」的情境。

建議新增以下工作表，且不要取代既有 `tenants`、`meter`、`invoices` 欄位：

#### `company_accounts` (公司戶帳單對象)
*   `account_id`：公司戶代號，例如 `HANMIN`。
*   `account_name`：帳單名稱，例如 `漢民公司`。
*   `contact_name`：帳務窗口，例如 `王小姐`。
*   `contact_line_user_id`：公司窗口的 LINE ID，可為空。
*   `rent_mode`：租金模式，例如 `external_summary`，代表租金沿用原本總表或公司帳務表，不由每房租金重新加總。
*   `fixed_rent`：固定租金，若租金由原本總表處理，可留空或只作備註。
*   `status`：`active` 代表啟用。

#### `company_room_map` (公司戶房號對照)
*   `yyyymm`：適用月份，若長期有效可填 `ALL`。
*   `room_id`：完整房號，例如 `楠梓A館107`。
*   `account_id`：公司戶代號，例如 `HANMIN`。
*   `include_meter`：是否列入水電明細，預設 `TRUE`。
*   `include_rent`：是否列入逐房租金，漢民原則上為 `FALSE`，避免重複計租。
*   `note`：備註，例如「漢民楠梓館 B」。

#### `company_meter_details` (公司戶水電明細)
此表由系統自動產生，每一列代表公司戶其中一間房的當期水電明細。

*   `yyyymm`：計費月份。
*   `account_id`：公司戶代號。
*   `room_id`：房號。
*   `tenant_name`：來源表上的姓名或遮罩姓名。
*   `prev_kwh`：上期度數。
*   `curr_kwh`：本期度數。
*   `used_kwh`：使用度數。
*   `kwh_price`：每度電費單價。
*   `electric_fee`：電費小計。
*   `people`：水費人數欄，例如 `單`、`雙`，或換算後人數。
*   `water_fee`：水費。
*   `total_fee`：水電合計。
*   `source_sheet`、`source_row`：來源工作表與列號，方便追查。

#### `company_invoices` (公司戶帳單彙總)
此表由 `company_meter_details` 彙總產生，每一列代表一個公司戶當月的一張帳單。

*   `yyyymm`：計費月份。
*   `account_id`：公司戶代號。
*   `account_name`：帳單名稱。
*   `contact_name`：帳務窗口。
*   `room_count`：本期明細房數。
*   `rent_amount`：租金金額。漢民租金沿用原本「公司帳務」或原始總表格式，不在水電明細中重算。
*   `electric_total`：電費合計。
*   `water_total`：水費合計。
*   `utility_total`：水電合計。
*   `grand_total`：總金額。
*   `export_file_url`：匯出的明細檔連結。
*   `status`：例如 `draft`、`exported`、`sent`、`paid`。

---

## 三、 漢民公司戶水電處理規格

### 1. 設計原則
漢民公司的租金已由原本 2026 總表與 `公司帳務` 表處理，因此自動化系統不應重新以每房租金加總。漢民新增的重點只有水電明細：

1.  原本租金格式維持不變。
2.  每一間漢民房間仍需保留一列水電明細。
3.  對外只輸出一份漢民水電明細檔，交給同一位負責人。
4.  不要求漢民每一間房各自綁定 LINE。
5.  若未來要透過 LINE 或 Email 發送，只發給 `company_accounts` 內的帳務窗口。

### 2. 水電來源
目前水電來源可分為兩種：

1.  外部 2026 總表中的月份水電表，例如 `2026.03`、`2026.04` 等。
2.  自動化系統中的 `src_meter` / `meter` 表。

系統應以 `src_meter` 或指定月份來源表為中繼資料，整理成標準欄位後再寫入 `company_meter_details`。不要讓 `company_invoices` 直接從原始月份表大量查詢，避免來源格式變動時影響帳單。

### 3. 漢民資料產生流程
每月抄表完成後，漢民公司戶流程如下：

1.  管理員完成原始水電表填寫。
2.  系統同步 `src_meter` 與 `meter`。
3.  系統依 `company_room_map` 找出屬於 `HANMIN` 的房號。
4.  從 `meter` 或標準化後的水電資料中抓取這些房號的水電資料。
5.  將每房資料寫入 `company_meter_details`。
6.  彙總 `company_meter_details`，產生 `company_invoices`。
7.  匯出一份「漢民公司水電明細」Excel / Google Sheets 檔。
8.  管理員確認後再傳給漢民負責人。

### 4. 漢民輸出檔格式
輸出給漢民的明細檔建議維持接近原本人工整理格式：

| 房號 | 姓名 | 原度數 | 新度數 | 使用度數 | 單價 | 小計 | 人數 | 水費(月) | 總金額 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- | ---: | ---: |
| 107 | 漢OO | 22278 | 22346 | 68 | 5 | 340 | 單 | 200 | 540 |
| 110 | 漢OO | 15488 | 15577 | 89 | 5 | 445 | 單 | 200 | 645 |

檔案最上方可保留標題，例如：

```text
2026年03.04月 漢民公司水電費
```

### 5. 檢查條件
產生漢民水電明細前，系統需檢查：

*   `company_room_map` 中的漢民房號是否都能在 `meter` 找到本期資料。
*   `curr_kwh` 是否大於或等於 `prev_kwh`。
*   `used_kwh` 是否異常過高。
*   是否有房號重複列入漢民明細。
*   輸出房數是否與本期預期房數一致。

若檢查失敗，系統不可直接發送，需寫入 `logs` 並標示錯誤房號。

---

## 四、 網頁管理後台操作指引

後台網址為 Google Apps Script 部署之 Web App。

### 1. 登入系統
*   開啟網址後，輸入管理密碼：`073524371`。

### 2. 儀表板 (Dashboard)
*   **數據總覽**：顯示總房間數、出租率、空房數及本月應收總額。
*   **待辦清單**：自動彙整「未繳租名單」、「合約即將到期（30天內）」與「待處理報修任務」。
*   **各館出租率**：以長條圖視覺化呈現各棟大樓的出租表現。

### 3. 日常核心操作：資料同步與帳單生成
每月抄表完成後，請依序執行以下操作：
1.  **確認抄表數值**：確認外部「抄表登記表」已填寫完畢。
2.  **同步資料**：點選網頁右上角的 **「🔄 同步資料」** 按鈕。
    *   系統會將最新的房客與電表資料寫入 `tenants` 與 `meter` 表。
    *   系統自動計算「本期 - 上期 = 使用度數」，並換算電費。
    *   系統自動在 `invoices` 表生成本月新帳單。

### 4. 公司戶操作：漢民水電明細輸出
漢民公司戶不走一般房客逐房發送流程。每月資料同步後，請依序執行：

1.  確認 `company_room_map` 中漢民房號是否正確。
2.  確認 `meter` 已有本期所有漢民房號資料。
3.  執行「產生公司戶水電明細」。
4.  檢查 `company_meter_details` 是否每一間房都有明細。
5.  檢查 `company_invoices` 的電費、水費、房數合計。
6.  匯出「漢民公司水電明細」檔案。
7.  管理員確認後，再傳給漢民負責人。

---

## 五、 LINE 官方帳號（房客端）互動說明

### 1. 房客自助綁定 LINE（按鈕選單化）
為防打字出錯，系統支援免輸入的按鈕式綁定：
1.  房客加入官方 LINE，並在對話框輸入「**綁定**」或「**開始**」。
2.  **選擇館別**：機器人自動回覆該帳號下所有啟用中的館別按鈕（如：`楠梓A館`、`楠梓B館`...）。
3.  **選擇房號**：房客點選館別後，機器人會查詢該館內**目前所有尚未綁定 LINE 的空房**，並彈出房號按鈕（如：`A1`、`A2`...）。
4.  **綁定成功**：房客點選房號後，系統自動將房客的 LINE ID 寫入試算表，並回傳綠色勾勾的「建檔成功」訊息。

### 2. 每月自動發送帳單
*   **執行時間**：系統設定於**每月 1 號上午 09:00** 自動觸發發送流程。
*   **安全驗證**：發送前，系統會自動檢查是否所有啟用中房客都有電表度數。若有漏填或異常，系統會拒絕發送並寫入錯誤日誌。
*   **帳單格式**：發送給房客的 LINE 訊息包含：
    *   計費月份、房號與房客姓名。
    *   房租費用。
    *   電表明細（上期度數、本期度數、使用度數、單價、電費總計）。
    *   水費明細。
    *   總計應繳金額與繳納截止日。
    *   轉帳提醒文案。

### 3. 公司戶發送原則
公司戶不使用一般房客逐房 LINE 綁定機制。以漢民公司為例：

*   每一間房仍保留水電明細。
*   帳單與明細只發給公司帳務窗口。
*   若 `company_accounts.contact_line_user_id` 為空，系統只產生匯出檔，不自動發送。
*   發送前需通過公司戶檢查條件，避免漏房或度數異常。

---

## 六、 管理員常見問題與維護

### Q1：如何手動補登過去的帳單（如 5、6 月份）？
1.  將外部「登記表」的數值改為該月份底的數值。
2.  在系統試算表的 `config` 表中，將 `yyyymm` 設為該月份（例如 `2026-05`）。
3.  至後台網頁點選 **「同步資料」**，即可在 `invoices` 建立該月歷史帳單。
4.  完成後將 `yyyymm` 改回最新月份。

### Q2：為什麼有房客收不到帳單？
*   請檢查 `tenants` 工作表中該房客的 `line_user_id` 欄位是否為空。若為空，代表房客尚未完成上述的 LINE 綁定步驟。

### Q3：不小心同步錯資料，如何還原？
*   本系統每次同步前皆會自動備份。若需還原，可在 Google 試算表選單中點選 **「自訂選單」 -> 「還原系統備份」**，資料將會回復至上一次同步前的狀態。

### Q4：漢民公司的租金要怎麼處理？
*   漢民租金維持原本 2026 總表與 `公司帳務` 的處理方式。自動化系統新增的公司戶流程只負責整理每房水電明細與公司戶彙總，不重新用每房租金計算漢民總租金。

### Q5：漢民水電明細是否要每一間房一個分頁？
*   資料庫內建議採「每房一列」，方便查詢、檢查與彙總。若漢民負責人需要特殊格式，可在匯出階段再轉成一個檔案內的多分頁，或依館別拆分。
