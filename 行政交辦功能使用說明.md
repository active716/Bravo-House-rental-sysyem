# 行政交辦功能使用說明

## 完成範圍

本次新增第一版行政交辦功能，直接放在現有網頁：

`https://active716.github.io/Bravo-House-rental-sysyem/`

功能包含：

- 新增行政交辦事項。
- 指派行政或一線人員。
- 設定截止日期。
- 狀態追蹤：待處理、處理中、完成。
- LINE 新增任務推播。
- 截止日當天提醒。
- 逾期未完成每天提醒。
- 完成後通知管理員。

第一版暫不做完整權限隔離。

## 使用方式

1. 進入網頁後，打開側邊欄的「行政交辦」。
2. 先在「員工 LINE 綁定」新增人員，填姓名、角色、綁定代碼。
3. 請員工到 LINE 傳：`員工綁定 綁定代碼`。
4. 綁定完成後，管理員新增交辦事項，指定被交辦人與截止日期。
5. 被交辦人從網頁把狀態改成「處理中」或「完成」。

## Apps Script 修改位置

### 行政交辦 Web App 後端

本機可確認檔案：

`C:\2605 anti poject\260531 租賃系統網站測試\apps-script\Code.gs`

替換方式：

- 將此檔內容部署到目前維修同步 Web App 對應的 Apps Script 專案。
- 重新部署 Web App 新版本。

影響範圍：

- 新增或更新 `admin_tasks` 分頁。
- 新增或更新 `staff_contacts` 分頁。
- 原本 `repairs` 維修同步仍保留。

### LINE 員工綁定

本機可確認檔案：

`C:\2605 anti poject\260531 租賃系統網站測試\apps-script\AdminTaskStaffLineBinding.gs`

新增方式：

- 貼到目前接 LINE webhook 的 Apps Script 專案。
- 在既有 `doPost(e)` 的 `events.forEach(function(event) { ... })` 裡，放在房客/帳單綁定邏輯之前：

```javascript
var adminTaskBind = tryHandleAdminTaskStaffBindingEvent_(event);
if (adminTaskBind.handled) return;
```

影響範圍：

- 只處理員工傳「員工綁定 綁定代碼」。
- 只寫入 `staff_contacts.line_user_id`。
- 不碰房客 LINE 綁定，不碰 `billing_accounts.contact_line_user_id`。

## 截止提醒排程

部署 `Code.gs` 後，在 Apps Script 手動執行一次：

```javascript
installAdminTaskReminderTrigger()
```

這會建立每天早上 8 點的提醒排程。

如果要手動測試提醒，可直接執行：

```javascript
sendAdminTaskDeadlineReminders_()
```

## 驗收方式

- 新增一位測試員工，完成 LINE 綁定。
- 新增一件今天截止的交辦事項。
- 確認被交辦人收到新增任務 LINE。
- 手動執行 `sendAdminTaskDeadlineReminders_()`，確認今天截止提醒只發一次。
- 新增或修改一件已逾期未完成事項，確認逾期提醒會發給被交辦人。
- 被交辦人把狀態改成「完成」，確認管理員收到完成通知。

停止條件：

**網頁可交辦、可追蹤、截止日提醒與逾期提醒都成功跑過一次。**
