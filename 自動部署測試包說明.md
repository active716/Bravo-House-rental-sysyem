# 自動部署測試包說明

更新日期：2026-06-14

## 目的

這次先做一個簡單可測試的 sandbox 版本，用來驗證「維修同步 / Apps Script 部署 / 手機端背景同步」流程，不直接改正式資料。

## 已建立內容

### 1. Sandbox Google Sheet

名稱：

```text
Bravo House 維修同步自動部署測試_SANDBOX_20260614
```

網址：

```text
https://docs.google.com/spreadsheets/d/1PuYj0_e1wGYfCkC6Z3mKRMChF6CN3oXVfOqARAAFSCc/edit
```

正式維修資料庫沒有被修改。

### 2. Sandbox 測試包

位置：

```text
sandbox/repair-sync-test/
```

內容：

- `Code.gs`：獨立 Apps Script 後端，只寫入 sandbox Sheet。
- `index.html`：手機端同步測試頁。
- `appsscript.json`：Apps Script manifest 範例。
- `README.md`：部署與測試說明。
- `scripts/check-sandbox-package.ps1`：本機 dry-run 檢查。

### 3. GitHub Actions Dry Run

位置：

```text
.github/workflows/sandbox-repair-sync-dry-run.yml
```

用途：

- 檢查 sandbox package。
- 檢查 `Code.gs` 基本語法。
- 檢查測試頁 `<script>` 基本語法。

這個 workflow 不會部署 Apps Script，不需要 Google 授權，也不會碰正式資料。

## 本機已完成檢查

已執行：

```powershell
powershell -ExecutionPolicy Bypass -File .\sandbox\repair-sync-test\scripts\check-sandbox-package.ps1
```

結果：

```json
{
  "ok": true,
  "sandboxSheetId": "1PuYj0_e1wGYfCkC6Z3mKRMChF6CN3oXVfOqARAAFSCc",
  "productionSheetReferenced": false,
  "version": "sandbox-2026-06-14-mobile-sync-v1"
}
```

## 如何測試

1. 打開 sandbox Google Sheet。
2. 進入「擴充功能」→「Apps Script」。
3. 貼上 `sandbox/repair-sync-test/Code.gs`。
4. 部署成 Web App。
5. 設定：

```text
執行身分：我
誰可以存取：任何人
```

6. 複製 Web App URL。
7. 打開測試頁：

```text
sandbox/repair-sync-test/index.html
```

8. 貼上 Web App URL。
9. 點「檢查 health」。
10. 新增測試維修，確認背景同步與讀取。

## 下一步建議

如果 sandbox 測試穩定，再做下一階段：

```text
GitHub Actions + clasp 自動部署 Apps Script
```

那一步會需要 Google 授權與 GitHub Secrets，所以先用 sandbox 驗證流程比較安全。
